# Series artwork

Temporary covers pulled from the church Subsplash CDN (org `ZZ5DWR`) for series we have already opened. Other series use a typographic tile until their art is copied.

Swap a file, keep the filename, or change `cover` in `src/data/media-catalog.json`.
